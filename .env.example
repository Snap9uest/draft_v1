# SnapQuest — .env.local 로 복사해서 채운다

# Gemini (AI 미션·인증·칭호). 없으면 전부 프리셋 폴백으로 동작한다.
GEMINI_API_KEY=

# Supabase — 프로젝트 설정 > API 에서 복사
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 서버 라우트 전용. NEXT_PUBLIC_ 접두사를 붙이지 말 것 — 브라우저에 노출된다.
SUPABASE_SERVICE_ROLE_KEY=
